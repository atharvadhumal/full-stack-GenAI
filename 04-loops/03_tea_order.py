orders = ["hitesh", "atharva", "ronaldo"]

for name in orders:
    print(f"Tea order for {name}")
    
    #short explaintion for this code:This code defines a list called `orders` that contains the names of three individuals: "hitesh", "atharva", and "ronaldo". It then uses a `for` loop to iterate through each name in the `orders` list. For each name, it prints a message indicating that there is a tea order for that person.